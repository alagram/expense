- flash.each do |name, msg|
  %div.col-md-10.col-md-offset-1(class="alert #{bootstrap_class(name)}")
    %a.close(data-dismiss='alert') &#215;
    = content_tag :div, msg, :id => "flash_#{name}" if msg.is_a?(String)
